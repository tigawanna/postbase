import { PageProps} from "rakkasjs";


export default function HomePage({}: PageProps) {
return (
    <main className="flex items-center justify-center w-full max-h-screen h-full gap-3">
      <h2 className="text-3xl font-bold">POSTBASE</h2>
    </main>
  );
}
